{
  "built": [
    "<code>rastrillo new</code> / <code>generate</code> — scaffold and route",
    "<code>rastrillo dev</code> — save, and it regenerates, rebuilds, restarts",
    "Manifests: a declared <code>Resource</code> generates store, actions, screens, and locale keys — declared filters, server-side <code>required</code> validation, and delete flows as their own confirm-page URL",
    "<code>examples/tickets</code> — a fully generated admin: zero hand actions, zero ejected templates",
    "<code>rastrillo.Run</code> — the full platform activation contract, hibernate and systemd tenants included",
    "SQLite bootstrap: pragma ordering, single-writer, additive migrations",
    "Localization: locale-resolved routes, <code>T</code>/<code>Tf</code> with fallback, a ship-gate catalog check",
    "<code>rastrillo/ui</code> — the component vocabulary: 27 partials across list, form, display and route families, documented class idioms, design tokens — light and dark, WCAG AA enforced by tests, zero JavaScript",
    "<code>examples/blog</code> — a real blog from stock components, coexisting with hand-written actions",
    "<code>rastrillo/crypto</code> — the family envelope: P-256 sealing and signing with a WebCrypto JS twin, proven against amadan's golden vectors",
    "<code>rastrillo/auth</code> — sign in with Keymail, magic-link email fallback, sessions, CSRF — over <code>keymaildev/signin</code>",
    "<code>rastrillo/webauthn</code> — passkeys, verify-only, with a test authenticator and the browser module included",
    "<code>rastrillo/eventlog</code> — the mergeable store shape: append-only streams, pure <code>Derive</code>, deterministic merge pinned by vectors",
    "<code>rastrillo/blobs</code> — content-addressed bytes on the platform's object store, presigned URLs, sealed E2EE wrapper",
    "Agents: actions opt in as tools; the generated registry dispatches consent-gated, actor-attributed calls; sidecars speak the platform contract"
  ],
  "notyet": [
    "Richer manifest kinds (bool, time, select, blob) and derived fields",
    "Mergeable manifest resources (the store shape is built; the generator wiring and edge sync are not)",
    "Step-up auth, and the crypto core's invite derivation"
  ]
}
